<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8" />
  <title><%= (post && post.title) ? (post.title + ' - 게시판') : '게시글 상세 - 게시판' %></title>
  <link rel="stylesheet" href="/styles.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
</head>
<body>
  <div class="container">
    <% if (post) { %>
      <div class="header">
        <h1 class="title"><%= post.title %></h1>
      </div>
      <div class="meta">작성자: <strong><%= post.author %></strong> • 작성일: <%= createdAt || (new Date(post.createdAt).toLocaleString()) %></div>
      <div class="card content"><%= post.content %></div>
    <% } else { %>
      <h1 class="title not-found">게시글을 찾을 수 없습니다.</h1>
    <% } %>

    <nav class="pagination">
      <a class="button" href="/board/list">목록으로</a>
      <% if (post) { %>
        <a class="button" href="/board/list/<%= post.id %>/edit">수정</a>
      <% } %>
    </nav>

    <div class="footer">Powered by Express + EJS</div>
  </div>
</body>
</html>
